{% extends 'base.html.twig' %}

{% block title %}Mon panier{% endblock %}

{% block body %}
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-2xl font-semibold text-gray-900 mb-8">Mon panier</h1>
    
    {% if cart|length > 0 %}
        <!-- Contenu du panier -->
        <div class="bg-white shadow overflow-hidden sm:rounded-lg">
            <!-- ... contenu du panier ... -->
        </div>
    {% else %}
        <div class="text-center py-12">
            <p class="text-gray-500">Votre panier est vide</p>
            <a href="{{ path('app_home') }}" class="mt-4 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700">
                Découvrir les cours
            </a>
        </div>
    {% endif %}
</div>
{% endblock %} 